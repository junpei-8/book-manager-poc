{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./base.json",
  "include": ["${configDir}/**/*", "${configDir}/.astro/**/*"],
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "jsx": "preserve"
  }
}
